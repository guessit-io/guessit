language: python
python:
  - 2.6
  - 2.7
  - 3.3
install:
  - pip install --allow-all-external --allow-unverified babelfish babelfish -r requirements.txt
  - pip install coveralls
script: coverage run --source=guessit setup.py test
after_success:
  - coveralls
